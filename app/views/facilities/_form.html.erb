<%= form_with(model: facility) do |form| %>
  <% if facility.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(facility.errors.count, "error") %> prohibited this facility from being saved:</h2>

      <ul>
        <% facility.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :capacity %>
    <%= form.number_field :capacity %>
  </div>

  <div class="field">
    <%= form.label :whiteboard %>
    <%= form.number_field :whiteboard %>
  </div>

  <div class="field">
    <%= form.label :conferencing %>
    <%= form.check_box :conferencing %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
